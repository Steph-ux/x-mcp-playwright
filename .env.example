# Run browser headed (visible window). Default: 1 (headless)
X_MCP_HEADLESS=1

# Default action timeout in ms
X_MCP_TIMEOUT_MS=20000

# Directory where debug screenshots get saved
X_MCP_SCREENSHOT_DIR=~/.x-mcp-playwright/screenshots
